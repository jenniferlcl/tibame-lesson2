{
  "name": "tibame-lesson2",
  "version": "0.1.0",
  "description": "Node.js agent skill practice project",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm run dev --prefix apps/backend\" \"npm run dev --prefix apps/frontend\"",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js",
    "test:watch": "node --experimental-vm-modules node_modules/jest/bin/jest.js --watch",
    "prepare": "husky"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "concurrently": "^10.0.3",
    "eslint": "^10.4.0",
    "husky": "^9.0.0",
    "jest": "^30.4.2"
  }
}
