'use strict';

const VEHICLE_STATUSES = ['available', 'in_use', 'maintenance', 'retired'];
const DEPARTMENTS = ['業務部', '工程部', '管理部', '資訊部', '財務部'];
const USER_ROLES = ['admin', 'user'];

module.exports = { VEHICLE_STATUSES, DEPARTMENTS, USER_ROLES };
